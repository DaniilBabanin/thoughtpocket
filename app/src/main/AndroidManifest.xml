<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Importing audio files into transcripts (no mic) runs as a data-sync foreground job. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- One-time model download only; all transcription is on-device. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ThoughtPocketApp"
        android:allowBackup="false"
        android:largeHeap="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThoughtPocket"
        tools:targetApi="34">

        <!-- Required (Android 12+) so the LiteRT GPU/NPU delegates can dlopen vendor libs. -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <uses-native-library android:name="libvndksupport.so" android:required="false" />
        <uses-native-library android:name="libcdsprpc.so" android:required="false" />
        <uses-native-library android:name="libedgetpu_litert.so" android:required="false" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.ThoughtPocket">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Share sheet: audio shared to the app becomes transcribed note(s). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/*" />
            </intent-filter>
        </activity>

        <!-- Headless permission bridge for the widget/tile. -->
        <activity
            android:name=".service.PermissionActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <service
            android:name=".service.RecordingService"
            android:exported="false"
            android:foregroundServiceType="microphone|dataSync" />

        <!-- One-time model downloads keep running when the app is backgrounded. -->
        <service
            android:name=".service.DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Coder feature: executes LLM-generated Python out-of-process so a hung
             script is killable (same-UID Process.killProcess from the client).
             isolatedProcess was spiked and is a hard no: Chaquopy's AndroidPlatform
             needs SharedPreferences, which isolated processes cannot access
             ("UserManager is not available") — see docs/coder-throughput.md. -->
        <service
            android:name=".coder.PyRunnerService"
            android:exported="false"
            android:process=":coder" />

        <!-- Coder session driver: generate → run → auto-fix loop with the model
             kept warm across follow-ups. dataSync like the app's other long work. -->
        <service
            android:name=".service.CoderRunService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".service.RecordTileService"
            android:exported="true"
            android:icon="@drawable/ic_notify"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
        </service>

        <receiver
            android:name=".widget.RecordWidget"
            android:exported="true"
            android:icon="@drawable/ic_notify"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_record_info" />
        </receiver>

    </application>
</manifest>
