# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android Studio / IntelliJ
.idea/
*.iml
.cxx/
captures/

# Local SDK paths (machine-specific)
local.properties

# Native build artifacts
app/.cxx/

# Logs
*.log

# Signing — the public release keystore IS committed on purpose so CI and
# local builds sign identically with no secrets (password is "whispershare").
# Everything else keystore-shaped stays out.
*.keystore
*.jks
keystore.b64
!app/release.keystore

# OS
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/
tmp-gecko/

# Cached on-device AI models (multi-GB, licence-gated Gemma weights + Gecko).
# Pulled from a device with adb; repopulate via tools/copy-edge-model.sh, then push
# to other devices with tools/push-models.sh. Never committed.
/models/

# HTML/CSS reference prototype for the Reach redesign — kept on disk, never committed.
/design/
